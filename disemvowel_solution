function disemvowel( $string )
    {
        $vowels = array("a", "e", "i", "o", "u", "A", "E", "I", "O", "U");
        $string = str_replace($vowels, '', $string);
        return $string;
    }

    disemvowel('aeiou');
